---
title: Configure launches
description: Define the launch catalogue the engine builds forecasts for.
---

import { Aside, Steps } from "../../site/src/components/docs/starlight";
import { SITES_SCHEMA_VERSION } from "@azohra/meteo.briefing/contract";

A **launch** is the place you fly; a **site** is that launch's record in
the data — this page authors those records. (Not installed yet? The
[section landing](/docs/forecast/) carries the install command and the
Node 22 requirement.) Every builder reads the same
site catalogue. Pass its path with `--sites`;
the CLI resolves it to an absolute path for that dispatch, so the file lives
wherever your project keeps it.

## Catalogue shape

The catalogue is **identity and build selection
only**: humans author WHERE, and nothing physical. The forecast engine measures
elevation, terrain, and land cover at those coordinates and publishes them
in [`site-context.json`](/docs/briefing/site-context-document/).

```json title="sites.json"
{
  "schemaVersion": 2,
  "sites": [
    {
      "slug": "test-hill",
      "name": "Test Hill",
      "latitude": 49.0,
      "longitude": -117.0,
      "timeZone": "America/Vancouver"
    }
  ]
}
```

The catalogue must satisfy `sitesCatalogueSchema` from `@azohra/meteo.briefing/contract` or
the generated `briefing/schema/sites.schema.json` artifact — and the engine
holds it stricter than a reader would: `meteo forecast build` refuses
unknown fields and `elevationM` outright, where the reader contract merely
strips them. A field someone typed and the pipeline silently ignored is the
failure the strictness exists to catch. You publish it to
the dataset root **verbatim** — a build never copies it there
([Publish static output](/docs/forecast/static-output/#publish-the-dataset-root)
walks the root files).

| Field | Meaning |
| --- | --- |
| `schemaVersion` | Site-catalogue contract version; <code>{SITES_SCHEMA_VERSION}</code> |
| `slug` | Stable lowercase, hyphenated identity used in output paths |
| `name` | Reader-facing launch name |
| `latitude`, `longitude` | Decimal degrees |
| `timeZone` | IANA timezone used to read local days and clock times |

<Aside type="note" title="No elevation of any kind lives here">
  The forecast engine rejects any file that includes a hand-typed
  `elevationM` (or any other unknown field) with a pointer to the
  measured replacement. The launch elevation is `site-context.json`'s
  `elevation` pick, measured from ground observations at the catalogued
  coordinates; each profile records the sampled grid terrain separately as
  `site.modelElevationM`.
</Aside>

## Add a site safely

<Steps>

1. Choose a stable slug. Renaming it changes every profile and history path.
2. Record coordinates from an appropriate source for your launches. Do not
   record elevation anywhere; the engine measures it in step 4.
3. Declare the launch's IANA timezone. Local time changes day windows and the
   meaning of timing findings, so builders echo this value into each profile
   as `site.timeZone`; do not infer it from longitude in presentation code.
4. Run the one-shot terrain measurement and commit `site-context.json`
   together with the catalogue change, so the published elevation, terrain,
   and land-cover context stays joined to the sites it describes:

   ```sh title="Terminal"
   pnpm exec meteo forecast terrain --sites ./sites.json --output ./site-context.json
   ```

   The geospatial stack loads only when this command runs; a scheduled
   `forecast build` never pays for it. The command reads the `--sites`
   catalogue and rewrites the `--output` site-context file. Its shape,
   the elevation-pick priority, and its sources are in the
   [site context reference](/docs/briefing/site-context-document/).

</Steps>

The catalogue is done. Next in the path:
[Choose models](/docs/forecast/choosing-models/) picks the slug, and
[Run one model](/docs/forecast/run-one-model/) validates this file with a
dry run before anything fetches — an invalid catalogue fails there with the
offending field named, and a site outside the chosen model's sampling guard
fails the build with an out-of-grid error rather than publishing a clamped
boundary value.

The catalogue chooses sampling locations and their local clocks. It does not
choose audience, launch suitability, display windows, or access policy. Those
belong to the operator.
