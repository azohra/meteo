---
title: "Component gallery"
description: "Every element in the custom-elements binding, rendered live on a synthetic season: cards, instruments, charts, roses, seasons, trends, air, the fleet table, strips, and the primitive atoms."
---

import StationGallery from "../../site/src/components/station/StationGallery.astro";

Every exhibit on this page is the real `<meteo-*>` custom element from
`@azohra/meteo.station/elements`, upgraded in your browser and fed a
synthetic fleet generated on the spot — nothing is fetched, nothing is a
screenshot. The twelve sections are the reference dashboard's registry,
one section per component family; the toolbar's unit control re-labels
every printed speed, and Live pauses the republish loop. The
[React binding](/docs/station/react/) renders byte-identical DOM from the
same feed; [Custom elements](/docs/station/elements/) documents the
attributes and properties each tag below is exercising.

<StationGallery />
