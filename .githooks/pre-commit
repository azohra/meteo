#!/bin/sh
# The static gates, at the moment a commit forms: lint and format, under
# a second. The full battery runs at push (.githooks/pre-push) and
# before every publish.
cd "$(git rev-parse --show-toplevel)" || exit 1
exec pnpm run gates:static
