---
title: Getting started
description: Install the packages and take the shortest path to your first forecast, chart, or live station feed.
---

import { Steps } from '@astrojs/starlight/components';

One pipeline connects every path on this page: an engine publishes static
JSON documents, and packages read what it published.

```text
provider bytes
   │  GRIB2 + JPEG 2000, decoded by @azohra/meteo.grib and @azohra/meteo.j2k
   ▼
meteo forecast build  (@azohra/meteo.forecast)
   │
   ▼
static profile JSON  (manifest + site profiles on hosting you control)
   │
   ▼
@azohra/meteo.briefing
   validate → derive → scene → SVG
            └ analyze → compare
```

## Publisher

<Steps>

1. Install the forecast engine with Node 22 or later:

   ```sh title="Terminal"
   pnpm add @azohra/meteo.forecast
   ```

2. Create a versioned [launch catalogue](/docs/publish/configure-launches/).
3. [Choose a catalogued model](/docs/models/choosing/) whose declared domain,
   cadence, levels, and capabilities fit the publication.
4. Preflight one model without network access or writes:

   ```sh title="Terminal"
   pnpm exec meteo forecast build --model hrrr-conus --sites ./sites.json --output ./public/data --max-steps 2 --dry-run
   ```

   A successful preflight prints one line and writes nothing:

   ```text
   Would build hrrr-conus for 2 site(s) from /your/project/sites.json into /your/project/public/data, capped at 2 step(s).
   ```

5. Remove `--dry-run` and the temporary `--max-steps` cap only when ready for
   the provider's full data volume. Then [publish the static output](/docs/publish/static-output/)
   on infrastructure you control.

</Steps>

## TypeScript integrator

Install the ESM package and begin at its contract boundary:

```sh title="Terminal"
pnpm add @azohra/meteo.briefing
```

[Render a first Meteogram](/docs/briefing/render-first-meteogram/) covers the
complete contract → scene → SVG path. The package is headless: the same scene
and SVG code works in Node, workers, and browsers. Use
[`@azohra/meteo.briefing/transport`](/docs/briefing/transport/) when loading a
manifest/profile pair from independently cached static storage.

[Analyze a profile](/docs/briefing/analyze/) produces compact, auditable
statements instead of chart geometry. The single-document API keeps the
thresholds and source evidence behind each finding.

[Compare model profiles](/docs/briefing/compare/) reports agreement and
divergence across those findings while preserving each model's run, cadence,
elevation, and availability.

[History and run convergence](/docs/briefing/history/) reads the published
month archives and compares successive runs of one model — the forecast
capability's one server-side subpath (Node, Bun, Deno; not browsers).

The platform's sibling package `@azohra/meteo.station` covers the
[station capability](/docs/station/): live weather-station reading,
derivation, and display, with client, server, React, and custom-element
bindings — [its own getting started](/docs/station/getting-started/)
begins there.

## Decoder user

The decoders beneath the engine install on their own — no forecast
documents or engine required:

```sh title="Terminal"
pnpm add @azohra/meteo.grib
```

[`@azohra/meteo.grib`](/docs/grib/) decodes GRIB2 in pure TypeScript — rotated
and Lambert grids, complex packing, multi-field messages, NOMADS `.idx`
byte-range helpers — gated bit-for-bit against ecCodes. Its documentation
opens with a complete worked decode: a committed HRDPS fixture in, a 2 m
temperature at a named launch out.

The JPEG 2000 inside ECCC messages is decoded by
[`@azohra/meteo.j2k`](/docs/j2k/), the production codec behind the `j2k-node`
seam; it also installs on its own.

## Forecast engine contributor

Read [Forecast architecture](/docs/forecast/architecture/) before an
internal module. Provider facts are verified and recorded in
the living [feed reference](/docs/forecast/forecast-model-feeds/) before a
builder consumes them.

## Technical learner

Start with [Reading a Meteogram](/docs/briefing/reading-a-meteogram/). The
figures render from the repository's
[synthetic scenario corpus](https://github.com/azohra/meteo/blob/main/scenarios/README.md),
whose own document explains how each recipe isolates one relationship.
The [logbook](/logbook/) records how derivations and visual
conventions were established.
