<section class="station-section" id="table" aria-labelledby="table-title">
  <h2 id="table-title">The fleet, on one table</h2>
  <p class="station-note">
    One row per station, unavailable rows keep their geometry — no
    attributes at all, the provider supplies everything.
  </p>
  <meteo-station-table></meteo-station-table>
</section>
